printjson(
  db.orderMaster.aggregate([
    {
      $lookup: {
        from: "customerMaster",
        localField: "customerId",
        foreignField: "_id",
        as: "customer"
      }
    }
  ]).toArray()
)
